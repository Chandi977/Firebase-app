package com.example.logggin;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity
{
    EditText ed1,ed2,ed3;
    Button btn;
    SqlLiteHepler sqlLiteHepler;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        ed1=findViewById(R.id.ed1);
        ed2=findViewById(R.id.ed2);
        ed3=findViewById(R.id.ed3);
        btn=findViewById(R.id.butn);
        sqlLiteHepler=new SqlLiteHepler(this);
        btn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {

                String str1,str2,str3;
                str1=ed1.getText().toString();
                str2=ed2.getText().toString();
                str3=ed3.getText().toString();
                       if (str1.equals("")|| str2.equals("")|| str3.equals(""))
                       {
                           Toast.makeText(MainActivity.this,"You must Enter all the Fields  ",Toast.LENGTH_LONG).show();
                       }
                      else
                       {
                           if (str2.equals(str3)) {
                               Boolean usercheck = sqlLiteHepler.checkusername(str1);
                               if (usercheck == false) {
                                   Toast.makeText(MainActivity.this, "password is correct", 